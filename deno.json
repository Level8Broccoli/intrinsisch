{
  "tasks": {
    "test:sass": "deno task test:sass:format",
    "test:sass:format": "prettier --check .",
    "test:sass:compile": "sass --no-source-map ./src/index.scss"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@^0.224.0"
  }
}
