{
  "name": "intrinsisch",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "sass --no-source-map ./sass/index.scss ./dist/intrinsisch.css",
    "format": "prettier --write ./sass/**/*.scss",
    "lint": "stylelint --fix ./sass/**/*.scss"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "sass": "^1.75.0",
    "stylelint": "^16.4.0",
    "stylelint-config-standard-scss": "^13.1.0"
  }
}
